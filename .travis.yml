language: java

jdk:
  - openjdk11

os:
  - linux

jobs:
  include:
    - stage: build
      script: mvn clean compile
    - stage: test
      script: mvn test
    - stage: package
      script: mvn package